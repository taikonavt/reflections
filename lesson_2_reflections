What happens when you initialize a repository? Why do you need to do it?

It was created the .git file


How is the staging area different from the working directory and the repository? What value do you think it offers?

Working directory is just a directory where files put in. This does not mean they will be checked.


How can you use the staging area to make sure you have one commit per logical change?

You can put only one changed file in staging area.


What are some situation when branches would be helpful in keeping your history organized? How would branches help?

You can track changes you did. You can look them every time you want.


How do the diagrams help you visualize the branch structure?

It shows you by graph all brunches of your project.


What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

The result of merging is united commit of two branches. Because two branches merge in one.
